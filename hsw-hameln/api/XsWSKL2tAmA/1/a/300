Diese Aussage beschreibt den Prozess des Vergleichs einer IP-Adresse und einer dazugehörigen Subnetzmaske. Durch eine Art Logik, die als "Endvergleich" bezeichnet wird, kann man feststellen, wie die IP-Adresse und die Subnetzmaske interagieren. Bei einem logischen Endvergleich wird für jeweils zwei Bits, die beide den Wert 1 haben, eine 1 als Ergebnis ausgegeben. Dies wird genutzt, um zu ermitteln, welche Teile der IP-Adresse für das Netzwerk und welche für den Host (den spezifischen Computer im Netzwerk) bestimmt sind. In dem gegebenen Fall wird festgestellt, dass in den ersten beiden Oktetten (den ersten 16 Bits) der IP-Adresse und der Subnetzmaske jeweils nur dann eine 1 herauskommen kann, wenn schon in der IP-Adresse eine 1 steht. Das zeigt, dass diese Teile der IP-Adresse den Netzwerkteil darstellen.