 Wenn jetzt IBM ein IP -Netzwerk aus Klasse A bekommen hat, dann haben die damit eine Anzahl von knapp 17 Millionen IP-Adressen bekommen und selbst IBM wird keine 17 Millionen Rechner adressieren müssen.  Also verschwendet man damit oder hat man damit eine große Anzahl von IP-Adressen verschwendet.