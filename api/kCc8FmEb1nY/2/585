The text is saying that text such as "hi there" can be converted into a series of numbers which represent the characters in the text. This process is called encoding. For example, the letter "h" might be represented by the number 46 and the letter "i" could be represented by the number 47.  

The explanation is part of a wider context where the speaker is explaining the workings of a tool called ChatGPT, which is an artificial intelligence system that can generate text when given a prompt. The encoding process is part of how the AI models and generates text.