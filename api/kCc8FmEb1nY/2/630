The presenter is starting the process of building a language model using Transformer architecture, which is the same technology used by Chat GPT. They start by fetching the 'Tiny Shakespeare' dataset which the presenter will use to train the model. They are opening the dataset in preparation to work with it.