Andrej says that ChatGPT is a kind of AI system that can create varying answers for the same text-based task, meaning it doesn't always give the same response, but instead forms different replies. This is because it's a 'probabilistic system', which means it uses a kind of chance or randomness when coming up with responses.