The instructor is about to start working with the PyTorch library, a popular Python-based machine learning library, to convert the entire text data of Shakespeare into numbers or tokens. This tokenizing process is essential as machine learning models can only understand numbers, so every character in the Shakespeare's text needs to be converted into a corresponding unique number or token. These tokens will be later used to train the model.