Andrej explains that ChatGPT is a language model that is able to predict and generate a series of words in a sequence. It does this by completing a given starting point or prompt. Once it is given the start of a sequence, it can generate the rest in sequence until it reaches an outcome. This is why, when he gave it the same task twice, it generated two different but relevant responses.